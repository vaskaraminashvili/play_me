<?php

/*
 * 1. make filter maybe managable from model like turn on/of fields for filter
 * 2. in filter add date , and date range
 *
 *
 *
 * make a conention for vue js
 * for new modules create folder in both places
 * admin shared AND
 * admin pages
 * if the does not belong to anything put in comman
 * if the components can be grouped group it with the folder outside of COMMON
 *
 */